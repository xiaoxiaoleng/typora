```shell
#重新装载配置
/usr/local/nginx/sbin/nginx -s reload /usr/local/nginx/conf/nginx.conf
#指定加载的配置文件
sudo nginx -c /usr/local/etc/nginx/nginx.conf
#校验文件的正确性
sudo /usr/local/bin/nginx -t -c /usr/local/etc/nginx/nginx.conf

/usr/local/Cellar/nginx/1.21.3/bin/nginx

验证命令为 ./nginx -t

```

